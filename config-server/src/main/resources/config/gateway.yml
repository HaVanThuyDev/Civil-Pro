server:
  port: 8000

spring:
  application:
    name: gateway

  data:
    redis:
      host: localhost
      port: 6379

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

      httpclient:
        ssl:
          use-insecure-trust-manager: true

      routes:
        - id: block-actuator
          uri: no://op
          predicates:
            - Path=/actuator/**
          filters:
            - name: SetStatus
              args:
                status: 403

        - id: auth-service
          uri: lb://CIVIL-PRO-AUTH
          predicates:
            - Path=/civil/auth/**, /civil/users/**
          filters:
            - RewritePath=/civil/(?<segment>.*), /${segment}
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 5
                redis-rate-limiter.burstCapacity: 10
                key-resolver: "#{@authKeyResolver}"

        - id: citizen-service
          uri: lb://CITIZEN-SERVICE
          predicates:
            - Path=/civil/citizen/api/**
          filters:
            - RewritePath=/civil/citizen/(?<segment>.*), /${segment}
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 10
                redis-rate-limiter.burstCapacity: 20
                key-resolver: "#{@authKeyResolver}"

        - id: household-service
          uri: lb://HOUSEHOLD-SERVICE
          predicates:
            - Path=/civil/household/**
          filters:
            - RewritePath=/civil/household/(?<segment>.*), /${segment}
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 10
                redis-rate-limiter.burstCapacity: 20
                key-resolver: "#{@authKeyResolver}"

        - id: fluctuations-service
          uri: lb://FLUCTUATIONS-SERVICE
          predicates:
            - Path=/civil/fluctuations/**
          filters:
            - RewritePath=/civil/fluctuations/(?<segment>.*), /${segment}
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 10
                redis-rate-limiter.burstCapacity: 20
                key-resolver: "#{@authKeyResolver}"

        - id: statistical-service
          uri: lb://STATISTICAL-SERVICE
          predicates:
            - Path=/civil/statistical/**
          filters:
            - RewritePath=/civil/statistical/(?<segment>.*), /${segment}
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 10
                redis-rate-limiter.burstCapacity: 20
                key-resolver: "#{@authKeyResolver}"

        - id: synchronized-service
          uri: lb://SYNCHRONIZED-SERVICE
          predicates:
            - Path=/civil/synchronized/api/**
          filters:
            - RewritePath=/civil/synchronized/(?<segment>.*), /${segment}
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 10
                redis-rate-limiter.burstCapacity: 20
                key-resolver: "#{@authKeyResolver}"

        - id: vnpay-service
          uri: lb://VNPAY-SERVICE
          predicates:
            - Path=/civil/vnpay/**
          filters:
            - RewritePath=/civil/vnpay/(?<segment>.*), /${segment}
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 10
                redis-rate-limiter.burstCapacity: 20
                key-resolver: "#{@authKeyResolver}"

      globalcors:
        cors-configurations:
          '[/**]':
            allowedOriginPatterns:
              - "*"
            allowedMethods:
              - GET
              - POST
              - PUT
              - PATCH
              - DELETE
              - OPTIONS
            allowedHeaders:
              - "*"
            allowCredentials: true
            maxAge: 3600

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
    register-with-eureka: true
  instance:
    prefer-ip-address: true
    hostname: localhost
    instance-id: ${spring.application.name}:${server.port}

civil-pro:
  jwt:
    secret: civil-pro-secret-key-minimum-32-characters-long

logging:
  level:
    org.springframework.cloud.gateway: INFO
    reactor.netty: INFO